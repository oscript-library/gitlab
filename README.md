Библиотека для работы с gitlab из OneScript.

Пример использования:

```
#Использовать gitlab

gl.УстановитьСервер("https://сервер.gitlab");
gl.УстановитьТокен("токен.gitlab");

IDПроекта = 1;
ОткрытыеМР = gl.ОткрытыеМержРеквесты(IDПроекта);

Для Каждого МР Из ОткрытыеМР Цикл
    Сообщить(СтрШаблон("IID открытого запроса на слияние: %1", МР["iid"]));
КонецЦикла;
```
